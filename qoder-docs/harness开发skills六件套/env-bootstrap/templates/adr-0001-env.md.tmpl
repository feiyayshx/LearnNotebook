<!-- 占位符: 启用域清单=本次运行 Phase 2 挑选启用的域(如 D1-D6,逐项列出,与差距报告一致); 跳过域及原因=用户选择跳过的域及一句话原因(无则填「无」); 铁律内容=经用户确认的 P0 铁律条目(与 .qoder/rules/always-on-rules.md 同源); 日期=本 ADR 生成日期(YYYY-MM-DD,与 0000-index.md 索引行一致); 决策者=确认本次环境决策的用户姓名或角色。生成定稿时删除本注释行 -->
<!-- 本文件由 env-bootstrap 按 adr-template.md.tmpl 结构预填,落盘为 docs/decisions/adr/0001-adopt-qoder-harness-env.md,记录本次环境决策(对应 baseline.md D4-4 补齐方式) -->

# ADR-0001: 采用 Qoder Harness 环境基线

- 状态:accepted
- 日期:{{日期}}
- 决策者:{{决策者}}

## 背景与问题
项目引入 AI 协作开发,需要一套统一的协作环境。Agent 入口、项目铁律、变更管理、文档与决策体系若各自为政,协作口径无法对齐,环境状态也无法复检。需要决定以何种方式建立并维护这套环境基线。

## 备选方案
1. 手工按白皮书配置 — 灵活可控,但步骤多、易漏项,各项目执行口径不一,配完难以复检。
2. env-bootstrap 基线化 — 按基线清单自动体检与补齐,可复现可复检,但基线本身需随白皮书演进维护。

## 决策
选择 env-bootstrap 基线化,因为它把环境从「一次性手工配置」变成「可体检、可复现的基线」,漏项与口径漂移由清单兜底。本次运行的具体决策如下:

- 启用域:{{启用域清单}}
- 跳过域及原因:{{跳过域及原因}}
- P0 铁律:{{铁律内容}}

## 后果
- 正面:环境可体检可复现,新成员与新会话按同一基线快速对齐,二次体检即可发现漂移。
- 负面/代价:基线更新需同步维护,env-bootstrap 基线清单演进时,本项目需重跑体检并同步补齐。
