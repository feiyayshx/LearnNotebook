<!-- 占位符: 项目名=项目正式名称; 项目简介=一句话项目定位(Phase 2 交互收集); 技术栈=主要语言/框架/存储(Phase 0 探测结论); 铁律列表=P0 铁律条目(2-3 条,经用户确认,与 .qoder/rules/always-on-rules.md 同源); 生成日期=本文件生成日期(YYYY-MM-DD)。生成定稿时删除本注释行 -->
<!-- 本文件由 env-bootstrap 生成;增量补齐时仅新增缺失章节,不改动已有内容 -->

# {{项目名}} — Agent 协作入口

## 项目简介

{{项目简介}}

技术栈:{{技术栈}}

## P0 铁律(红线)

以下铁律经用户逐条确认,是本项目的最高优先级约束,**违反任意一条立即停止并上报**,不得自行补救后继续:

{{铁律列表}}

铁律的完整版本与维护入口见 `.qoder/rules/always-on-rules.md`,两处内容保持同源,修改须同步。

## 目录索引

| 目录 | 用途 | 说明 |
|------|------|------|
| `openspec/` | 变更管理 | openspec 变更提案、规格增量与归档,`config.yaml` 承载项目上下文 |
| `docs/specs/` | 规格文档 | spec-writing 产出的需求规格说明书统一落盘于此,是需求事实源 |
| `docs/decisions/adr/` | 架构决策 | ADR 记录重大决策及理由,含模板 `template.md` 与索引 `0000-index.md` |
| `docs/env/` | 环境体检 | env-bootstrap 的体检报告 `env-report.md` 与人工待办 `todo-manual.md` |
| `.qoder/rules/` | 项目规则 | 始终生效的 P0 铁律与提交前自检清单,Agent 每次会话自动加载 |
| `.qoder/rules/git-collaboration.md` | Git 协同约定 | 分支策略/提交规范/评审要求 |

## 开发流水线

本项目采用五环节流水线,按顺序衔接,每环节一句话说明何时使用:

- **env-bootstrap**:开始任何 AI 协作开发前,先准备/体检协作环境(本文件即其产物之一);
- **spec-writing**:接到需求雏形或原型时,澄清模糊点并撰写正式规格说明书,落盘 `docs/specs/`;
- **spec-review**:规格定稿前,对规格文档做一致性、完整性、可实现性评审并修订;
- **deliver-system**:规格确认后,按双轨工作流实现并交付可运行的系统或功能;
- **spec-loop-verify**:交付完成后,以规格为唯一依据做有界循环验证,全部通过才算验收。

## 文档分层原则

规则(`.qoder/rules/`)只放强约束短句,回答"什么不能做";docs(`docs/`)放背景与依据,回答"为什么这样做";spec(`docs/specs/`)是需求的唯一事实源,回答"要做成什么样";ADR(`docs/decisions/adr/`)记录决策及理由,回答"当时为什么这么选"。四者各司其职,**不互相复制内容**——需要引用时给链接,不粘贴正文,避免多处维护同一信息。开发中的经验教训按 always-on-rules P2 的回流约定沉淀回 rules/docs,使 Harness 随项目演进而非一次性配置。

---

生成日期:{{生成日期}}(由 env-bootstrap 生成)
