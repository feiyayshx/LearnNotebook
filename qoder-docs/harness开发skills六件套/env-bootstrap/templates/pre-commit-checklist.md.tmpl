<!-- 占位符: 项目名=项目正式名称。生成定稿时删除本注释行 -->
<!-- 本文件由 env-bootstrap 生成(白皮书附录 B 通用裁剪版);按技术栈裁剪:标注"不适用则删除此条"的条目按项目实际删改 -->

# {{项目名}} 提交前自检清单

每次提交前按 **P0 → P1 → P2** 顺序逐项勾选核对;P0 任一项未通过禁止提交,P1 未通过需在提交/PR 说明中写明原因,P2 为建议项。

### P0(任一未过禁止提交)

- [ ] 本次改动未触碰 P0 铁律划定的范围(见 `.qoder/rules/always-on-rules.md`);确需触碰的已停止并上报,取得授权后才继续。
- [ ] 无凭据、密钥、密码、token 等敏感信息入库,配置中只留占位符或引用环境变量。
- [ ] 涉及数据库变更时,SQL 为增量脚本且幂等可回滚(如使用 `IF NOT EXISTS`),不删不改已发布结构与数据——项目无数据库则删除此条。

### P1(未过需说明)

- [ ] Diff 全部在本次任务清单范围内,无顺便优化、顺便重构、顺便升级依赖。
- [ ] 相关测试已运行并通过(单元/集成按项目约定),新增逻辑有对应用例。
- [ ] 无格式化污染:未对本次改动之外的代码做重新格式化、调整缩进或整理 import。
- [ ] 本次如做出架构级决策,已在 `docs/decisions/adr/` 记录 ADR 并更新索引——无架构决策则勾选跳过。
- [ ] 本次改动涉及模块/表/对外接口/环境组件的新增、更名、删除时,已同步更新对应 `docs/maps/` 地图——未涉及则勾选跳过。

### P2(建议)

- [ ] 涉及架构、接口、配置、约定变更时,其余文档(AGENTS.md / docs/ 索引等,地图同步已由 P1 覆盖)已按需同步更新。
