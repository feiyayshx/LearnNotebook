<!-- 占位符: 生成日期=本清单生成/刷新日期(YYYY-MM-DD);待办标题=待办事项标题(取自 ide-manual-steps.md 对应节标题,如"市场 Skill 安装");检查项编号=对应基线检查项 Dx-y(如 D5-2,可选增强项写"无固定检查项");操作步骤=从 references/ide-manual-steps.md 对应节整段摘抄的编号步骤(精确到菜单路径,不改写);完成判据=同节"完成判据"原文照抄(一条命令或现象,不改写口径)。生成定稿时删除本注释行 -->
<!-- 本文件由 env-bootstrap Phase 3 生成:IDE 级操作 skill 无法代为执行,逐项写入本清单由用户手工完成;素材按 references/ide-manual-steps.md 逐节整段摘抄,不改写判据口径 -->

# 环境准备人工待办清单

> 由 env-bootstrap 生成于 {{生成日期}}。完成一项勾选一项,然后重新运行 /env-bootstrap 体检确认转 ✅。

- [ ] {{待办标题}}(对应检查项 {{检查项编号}})
  - 操作步骤:{{操作步骤}}
  - 完成判据:{{完成判据}}

<!-- 逐条待办复制上面三行结构,一条待办一个复选框;操作步骤保留原编号列表(多行时缩进对齐到"操作步骤:"之后),完成判据只写一条命令或现象。
示例(摘抄自 ide-manual-steps.md「Repo Wiki 生成」节):
- [ ] Repo Wiki 生成(对应检查项 D6-4)
  - 操作步骤:
    1. 用 Qoder 打开项目根目录(多仓形态打开工作区根),等待 Qoder 对仓库的自动扫描索引完成;
    2. 若 .qoder/repowiki/ 未自动生成,在 Qoder 内触发仓库扫描 / Repo Wiki 生成动作(可在会话中直接要求 Agent 生成或更新 Repo Wiki);
    3. 生成完成后抽查 Wiki 内容:模块拆解、架构概览、各模块技术栈与规范应与实际代码一致,明显过时或缺失时重新触发生成。
  - 完成判据:执行 `ls .qoder/repowiki/`,目录存在且非空、含模块条目文件。
-->
