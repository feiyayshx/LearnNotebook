<!-- 占位符: 项目名=项目正式名称; 铁律列表=P0 铁律条目(2-3 条,经用户确认); 动作=铁律禁止的具体动作(铁律列表条目内子占位符); 范围=铁律生效的模块/表/接口范围(铁律列表条目内子占位符); 豁免条件=允许例外的前置条件,无则写"无豁免"(铁律列表条目内子占位符)。生成定稿时删除本注释行 -->
<!-- 本文件由 env-bootstrap 生成,P0 内容经用户确认;修改需团队评审 -->

# {{项目名}} 项目规则(始终生效)

## P0 铁律

<!-- 每条铁律格式:1. **不{{动作}}** — {{范围}},除非 {{豁免条件}};本行为句式示例,填充铁律列表后删除本行 -->

{{铁律列表}}

违反任意一条 P0 铁律必须立即停止当前工作并上报,不得自行补救后继续。

## P1 强约束

1. **Diff 必须只包含本次需求范围内的改动**,禁止顺便优化、顺便格式化、顺便升级依赖;发现范围外问题单独登记,不夹带进本次提交。
2. **未经用户确认的内容不得写成"必须/禁止"级规则**,只能写成"建议"或登记到 `docs/open-questions.md` 待确认。
3. **禁止脑补需求**:凡是规格与用户输入未明确的行为,必须以"推测"标注并向用户确认后才能实现,不得按自己的理解直接写死。
4. **修改存量代码时必须在提交说明中标注兼容性验证点**,写明验证了哪些原有调用路径、如何保证旧逻辑不被破坏。
5. **功能开发必须先有对应规格再动代码**:新功能/行为变更必须存在对应的 spec(`docs/specs/`)或 OpenSpec change(`openspec/changes/`)并经用户确认;无规格的代码改动仅限缺陷修复与用户明确豁免的微调。
6. **架构级决策必须当次留痕**:技术选型、模块边界、数据结构、对外契约类决策,在决策发生的同一次交付内新增 ADR 到 `docs/decisions/adr/` 并更新 `0000-index.md`,不得事后补记。
7. **文档随动作保活**:动手前先查阅架构文档、`docs/maps/` 地图与相关日志确认现状;动作完成后同一次交付内更新受影响文档与地图(涉及模块/表/接口/环境组件变化必须同步对应地图);计划类工作先落盘仓库文件(spec/tasks/plan)再执行并同步状态,不得只存在于会话记忆。

## P2 建议

1. 修复问题时主动扫描同类问题,把发现的同类项列成清单交用户拍板,不擅自扩大修改范围。
2. 新增文件遵循项目既有目录约定与命名风格,不自创平行目录结构。
3. 迭代或缺陷修复完成后,将可复用经验(根因模式、规避方法)提炼为候选条目,经用户确认后回写 `.qoder/rules/` 或 `docs/`;一条经验一个提案,不夹带,不把一次性细节写成永久规则。
