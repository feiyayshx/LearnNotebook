<!-- 占位符: 项目名=项目正式名称; 分支策略=分支协作模型(Phase 2 交互二选一:主干直提交 / 功能分支+PR,默认功能分支+PR)。生成定稿时删除本注释行 -->
<!-- 本文件由 env-bootstrap 生成;「分支策略」章节按 Phase 2 选择从下方注释中取用对应成文段落,未选用的段落删除 -->

# {{项目名}} Git 协同约定

## 分支策略

本项目采用:**{{分支策略}}**

<!-- 分支策略=主干直提交 时取用以下段落:
直接在主分支小步提交:每次提交范围小、可独立回滚;每次提交前必须完整跑一遍 `.qoder/rules/pre-commit-checklist.md` 并全部通过,P0 任一项未过禁止提交。
-->
<!-- 分支策略=功能分支+PR 时取用以下段落:
每个变更在 `feature/{变更名}` 分支上开发({变更名} 对应 spec 或 openspec change 名);主分支只接受评审通过的合并,禁止直接向主分支提交业务改动。
-->

## 提交规范

采用 Conventional Commits 中文约定:提交信息以 `feat` / `fix` / `docs` / `chore` / `refactor` 之一为前缀,后接一句话说明本次范围,例如 `feat: 新增订单导出功能`。提交信息须能对应到 `docs/specs/` 的 spec、`openspec/changes/` 的 change 或明确的缺陷描述,不允许无来源的功能性提交。

## Agent 工作副本隔离

Agent 执行长任务(多文件改动/长交付)时,必须使用独立 worktree 或功能分支作为工作副本,禁止直接在主分支上做多步改动;任务完成后经评审合并回主分支,中间产物不遗留在主分支历史中。

## 评审与合并

- 合并前须通过 `.qoder/rules/pre-commit-checklist.md` 的 P0/P1 全部条目;
- 涉及 P0 铁律范围(见 `.qoder/rules/always-on-rules.md`)的改动,必须经人工评审后方可合并,不得仅由 Agent 自评通过。
